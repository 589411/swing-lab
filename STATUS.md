# SWING LAB 專案狀態

## 專案簡介
爵士鼓節奏練習 Web App（PWA），支援八聲部完整鼓組（Crash/Ride/HH/Tom×3/Snare/Kick）+ 互動式 SVG 鼓組視覺化、三連音網格、12 種內建節奏型、學生跨裝置紀錄同步。

## 當前狀態
✅ **完全上線** — https://swing.launchdock.app 可正常訪問
✅ **SSL 已啟用** — 強制 HTTPS，憑證到期 2026-10-10
⏸️ **Firebase 未啟用** — 雲端同步功能未設定，目前只存本機

## 已完成
- [x] GitHub repo 建立：https://github.com/589411/swing-lab
- [x] GitHub Pages 啟用（main branch / root）
- [x] CNAME 檔案已存在並被 Pages 識別（swing.launchdock.app）
- [x] Pages 建置完成（status: built）
- [x] Cloudflare DNS 設定完成（swing.launchdock.app → 589411.github.io）
- [x] SSL 憑證簽發完成
- [x] 強制 HTTPS 已啟用
- [x] 網站正常訪問驗證通過
- [x] 擴充到 8 聲部鼓組（新增 Crash/Tom1/Tom2/Tom3）
- [x] SVG 互動式鼓組視覺化（點擊發聲 + 動畫）
- [x] WebAudio 合成音色（Tom 和 Crash 音色）
- [x] **力度檔位模式**（2026-08-30）— 補上能力地圖 A3 力度控制與 D1 音量層次

## 力度檔位模式（新）
教會鼓手課程的能力地圖有 15 個「上台門檻」項目，在這之前一項都無法自動驗收。這個模式補上其中兩項。

**原理**：onset worklet 本來就把每一下的 RMS 跟時間一起送出來（`postMessage({onset, e})`），只是 `judgeHit` 把 `e` 丟掉了。接起來除以「最大力度參考值」正規化成 0~1，再跟目標檔位比。

**兩種目標曲線**
- 三檔：每 4 小節換一檔 30% → 60% → 100%，練 A3 L2「能穩定停在三個檔位」。
- 推上去：8 小節從 30% 線性推到 100%，練 A3 L3「八小節內線性推上去，不跳階、不加速」。

**四個指標**
- 每檔「平均實際 ±浮動」：浮動小＝停得住。
- 檔位命中率：|實際−目標| ≤ 0.12 的比例。
- 三檔分離度：高檔平均 − 低檔平均，三檔拉不拉得開。
- **大聲時搶拍**：低檔平均時間偏差 − 高檔平均時間偏差。正數代表一大聲就趕。這一項是把課程的核心句「溫度可以一直升，速度不可以動」變成一個數字，超過 ±8ms 會標紅。

**一個實際踩到的誤讀（2026-08-31 修正）**
Joseph 看到說明文字「每 4 小節換一檔 30→60→100」，**以為那是速度**，不是音量。原因很直接：這個區塊就排在「速度 90 BPM」滑桿的正下方，而在鼓的 App 裡最顯眼的數字本來就是 BPM。連提出這個功能的人都會這樣讀，學生一定也會。

修法是把單位講死，不要靠上下文：區塊改名「音量檔位」（沿用母文件 4.5 的稱呼）、按鈕改成「音量三檔／音量推上去」、每一列標籤都帶「音量」二字、說明第一句先講「這是音量的百分比，不是速度。速度全程不動——溫度可以一直升，速度不可以動」。最後那句同時是課程的核心句，順便把觀念釘在工具上。

**教訓**：數字沒有單位的時候，讀者會用畫面上最近的那個數字去補。

**限制（誠實記錄）**
- 只有麥克風輸入能用。空白鍵/點擊沒有振幅，切過去會自動關閉這個模式。
- 麥克風收的是整個房間，**量的是可重現性，不是絕對分貝**。「三檔分不分得開、守不守得住」有意義，「30% 是不是真的 30%」沒有意義。
- 容差 `LV_TOL = 0.12` 是我設的初值，**沒有用真鼓校準過**。實際教學時若發現太嚴或太鬆，改這一個常數即可。
- 校準取 8 下最大力、丟掉最小的兩下再平均。若學生第一下沒打實會被丟掉，是刻意的。

**驗證方式**：注入已知振幅與已知時間偏差，比對七項統計輸出與手算值，全部相符；目標曲線（三檔 12 小節、推上去 8 小節）逐小節比對正確；切換輸入來源的守門與行內警告正確；頁面載入零 console error。未用真麥克風＋真鼓驗證——**這是還沒做的那一段**。

## 意外模式（新，2026-08-31）
母文件 4.4 把「參考時間軸換成一首歌」跟「時間軸可以被破壞」綁成一個動作，整包卡在待決事項「壞掉的音檔素材從哪來」。**但那張表五列裡只有最後一列（段落標記＋提問→C 欄）真的需要歌**；前四列要的只是一條會被破壞的參考時間軸，而 scheduler 本來就在生成一條。所以先把不需要素材的四列做掉，素材決定可以繼續等。

| 模式 | 破壞方式 | 打開的能力 | 量出來的數字 |
|---|---|---|---|
| 速度會漂 | 參考時間軸自己變快變慢，偏向被拖慢 | B4 跟人不跟機器 / F3 速度崩壞 | 同時給「跟著漂」與「守住原速」兩個平均偏差 |
| 突然插拍 | 每 6~10 小節重拍位移 ±1~2 拍 | C2 上車能力 / F2 段落錯位 | 上車時間（幾拍之內重新命中） |
| 節拍器消失 | 四小節有聲、四小節靜音，判定照常 | B2 內在脈動 | 靜音段與有聲段的準度差 |
| （全模式） | — | F1 掉拍復原 | MISS 到下一次 PERFECT 的間隔，門檻 4 拍（一小節） |

**一個對母文件的修正**：4.4 寫「中間多插四小節」，但在循環的格子上每小節長得一樣，插整小節偵測不到。等效且測得到的是**插拍**——把重拍位移，學生要在下一個小節的第一拍重新上車，那正是 C2 的定義。等曲庫做出來、時間軸上有真的段落之後，才輪得到「多插四小節」。

**「速度會漂」為什麼要給兩個數字**：F3 要的是「不被拖走」，B4 要的是「判斷該跟還是該撐」——這兩個的正確答案相反，工具不能替學生選。所以參考時間軸照樣漂（那是「全團」），但同時顯示他對漂移軸與對原速軸的偏差，讓他看見自己剛剛做了哪一件。練習前由老師指定這次要練哪一個。

**實測抓到並修掉的兩個問題**
1. 漂移原本是無邊界隨機游走：12 次模擬跑 80 小節，7 次偏離超過 12 BPM，最誇張從 90 漂到 58——那不是被拖慢，是換了一首歌。改成有回歸力、硬邊界 ±8 BPM（`DRIFT_MAX`），重測 20 次跑 120 小節，最大偏離 4.8 BPM，平均終點 88（保留偏向被拖慢的傾向）。
2. 第一次插拍發生在第 0 小節，學生還沒進入狀態就被打亂。改成起始至少 6 小節後。

**驗證**：用假時鐘取代 AudioContext 直接驅動 scheduler（Chrome 的 autoplay policy 讓自動化測試無法啟動音訊，這條路走不通），逐項比對——小節推進、靜音循環、雙時間軸不互相污染、位移量為整數拍、漂移不動 `barDur0`、上車與復原拍數、關閉時完全不影響原有行為。**未用真人＋真鼓實際打過。**

## 卡點
**下一步需要真鼓驗證** — 上面的邏輯是用注入資料測的，`LV_TOL` 與校準取樣策略要用真鼓＋真麥克風跑一次才知道合不合用。

## 下一個具體動作
**帶真鼓跑一次力度檔位模式**，校準兩個數字：`LV_TOL`（目前 0.12）與校準取樣策略。判準是「三檔分離度」在真鼓上是否穩定拉得開（預期 50 個百分點以上）。

之後的分岔（見 church-drummer-training 母文件第 6 節）：
- 力度檔位已完成 → 下一個大工程是**把參考時間軸從節拍器換成一首歌，而且讓它可以被破壞**，一個動作同時打開能力地圖的 C 欄（曲式）與 F 欄（復原）。成本高，要準備曲庫與 beat map。
- 可選：Firebase 雲端同步（參考 DEPLOY.md §5），目前只存本機。

## 相關專案
`church-drummer-training`（private）— 教會鼓手訓練課程的能力地圖與教材。本工具是它的驗收層，改造方向由該 repo 的母文件第 4 節決定。

## 技術棧
- 單檔 HTML（index.html）+ PWA（manifest + sw.js）
- Firebase Firestore（可選，跨裝置同步）
- GitHub Pages + Cloudflare DNS

## 重要連結
- 線上網址：https://swing.launchdock.app
- GitHub repo：https://github.com/589411/swing-lab

## 最後更新
2026-08-31 — 新增意外模式（B2／B4／C2／F1／F2／F3）
